// 1. Run the server
// 2. Serve the ending off the file given on the / path
// Serving ending only because log files are usually really long and serving the whole file
// would be bad
pub fn host() {
    println!("Hosting the server and serving the file given... on port x");
}
